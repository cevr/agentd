---
"@cvr/agentd": minor
---

CLI audit: security fixes (ID validation, XML escaping), typed Schedule schema, `--json` output, improved error handling (recovery hints, defect logging), perf (Bun.which, parallel reads), dead code removal.
