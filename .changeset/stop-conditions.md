---
"@cvr/agentd": minor
---

Stop conditions for scheduled tasks: `--max-runs` and `--until`. Tasks auto-complete and uninstall when any condition is met (OR semantics). Also fixes lifecycle state not updating on failed agent runs.
