---
"@cvr/agentd": minor
---

Task context metadata: captures git branch, remote, commit, default branch, PR number/URL, and issue number at creation time. Injected into agent prompt as `<context>` block at invocation.
